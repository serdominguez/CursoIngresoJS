function mostrar()
{
	//alert('iteración while');
	/*
	var cont = 0;
	var nota;
	var acum = 0;
	var bandera = true;

	while (cont<5)
	{
		cont++;
		nota=prompt("ingrese nota");
		acum=acum+nota;
		//acum+=nota   es lo mismo que el caso de arriba

		if (bandera) 	//con esto se puede ejecutar una instruccion una sola vez (en la primer ejecucion)
		{
			bandera=false;
		}

		if (cond<5) 
		{
			continue;	//palabra reservada para volver a validar la condicion del while
			break;		//rompe el while
		}

	}
	

	var cont = 0;

	while (cont<10) 
	{
		cont++;

		console.log(cont);
	}

	*/var cont;


	var cont=0;

	do {
		cont ++
		console.log(cont);
	} while (cont<10);


}//FIN DE LA FUNCIÓN